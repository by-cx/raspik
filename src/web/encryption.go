package main

// GetConfig loads configuration from the system and returns it parsed into the right structure
func GetConfig(password string) (InstanceConfiguration, error) {
	return InstanceConfiguration{}, nil
}
